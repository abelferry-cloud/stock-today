<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stock.platform.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.stock.platform.entity.Message">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="tokens" property="tokens"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.stock.platform.entity.Message" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (conversation_id, role, content, tokens)
        VALUES (#{conversationId}, #{role}, #{content}, #{tokens})
    </insert>

    <select id="selectByConversationId" resultMap="BaseResultMap">
        SELECT id, conversation_id, role, content, tokens, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at ASC
    </select>

    <select id="selectByConversationIdWithLimit" resultMap="BaseResultMap">
        SELECT id, conversation_id, role, content, tokens, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="countByConversationId" resultType="int">
        SELECT COUNT(*)
        FROM message
        WHERE conversation_id = #{conversationId}
    </select>

</mapper>
