<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.me.stock.mapper.SysUserRoleMapper">

    <resultMap id="BaseResultMap" type="com.me.stock.pojo.entity.SysUserRole">
            <id property="id" column="id" />
            <result property="userId" column="user_id" />
            <result property="roleId" column="role_id" />
            <result property="createTime" column="create_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,role_id,create_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from sys_user_role
        where  id = #{id} 
    </select>
    <select id="getRolesIdByUser" resultType="java.lang.String">
        select role_id from sys_user_role where user_id = #{userId}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from sys_user_role
        where  id = #{id} 
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.me.stock.pojo.entity.SysUserRole" useGeneratedKeys="true">
        insert into sys_user_role
        ( id,user_id,role_id,create_time)
        values (#{id},#{userId},#{roleId},#{createTime})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.me.stock.pojo.entity.SysUserRole" useGeneratedKeys="true">
        insert into sys_user_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="userId != null">user_id,</if>
                <if test="roleId != null">role_id,</if>
                <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="userId != null">#{userId},</if>
                <if test="roleId != null">#{roleId},</if>
                <if test="createTime != null">#{createTime},</if>
        </trim>
    </insert>
    <insert id="addUserRole">
        insert into sys_user_role
        ( id,user_id,role_id,create_time)
        values
        <foreach collection="userRoles" separator="," item="role">
            (#{role.id,jdbcType=BIGINT},#{role.userId,jdbcType=BIGINT},#{role.roleId,jdbcType=BIGINT}
            ,now())
        </foreach>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.me.stock.pojo.entity.SysUserRole">
        update sys_user_role
        <set>
                <if test="userId != null">
                    user_id = #{userId},
                </if>
                <if test="roleId != null">
                    role_id = #{roleId},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.me.stock.pojo.entity.SysUserRole">
        update sys_user_role
        set 
            user_id =  #{userId},
            role_id =  #{roleId},
            create_time =  #{createTime}
        where   id = #{id} 
    </update>
</mapper>
