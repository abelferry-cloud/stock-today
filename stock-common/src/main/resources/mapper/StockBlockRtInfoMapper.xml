<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.me.stock.mapper.StockBlockRtInfoMapper">

    <resultMap id="BaseResultMap" type="com.me.stock.pojo.entity.StockBlockRtInfo">
            <id property="id" column="id" />
            <result property="label" column="label" />
            <result property="blockName" column="block_name" />
            <result property="companyNum" column="company_num" />
            <result property="avgPrice" column="avg_price" />
            <result property="updownRate" column="updown_rate" />
            <result property="tradeAmount" column="trade_amount" />
            <result property="tradeVolume" column="trade_volume" />
            <result property="curTime" column="cur_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,label,block_name,company_num,avg_price,updown_rate,
        trade_amount,trade_volume,cur_time
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from stock_block_rt_info
        where  id = #{id} 
    </select>
    <select id="sectorAllLimit" resultType="com.me.stock.pojo.domain.StockBlockDomain">
        select
            sbr.company_num  as companyNum,
            sbr.trade_amount as tradeAmt,
            sbr.label        as code,
            sbr.avg_price    as avgPrice,
            sbr.block_name   as name,
            sbr.cur_time     as curDate,
            sbr.trade_volume as tradeVol,
            sbr.updown_rate  as updownRate
        from stock_block_rt_info as sbr
        where sbr.cur_time=#{timePoint}
        order by sbr.trade_volume desc
        limit 10
    </select>
    <select id="findBlockInfoByTimeLimit" resultType="com.me.stock.pojo.domain.StockBlockDomain">
        select
            sbr.company_num  as companyNum,
            sbr.trade_amount as tradeAmt,
            sbr.label        as code,
            sbr.avg_price    as avgPrice,
            sbr.block_name   as name,
            sbr.cur_time     as curDate,
            sbr.trade_volume as tradeVol,
            sbr.updown_rate  as updownRate
        from stock_block_rt_info as sbr
        where sbr.cur_time>= #{startTime}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from stock_block_rt_info
        where  id = #{id} 
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.me.stock.pojo.entity.StockBlockRtInfo" useGeneratedKeys="true">
        insert into stock_block_rt_info
        ( id,label,block_name,company_num,avg_price,updown_rate,
        trade_amount,trade_volume,cur_time)
        values (#{id},#{label},#{blockName},#{companyNum},#{avgPrice},#{updownRate},
        #{tradeAmount},#{tradeVolume},#{curTime})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.me.stock.pojo.entity.StockBlockRtInfo" useGeneratedKeys="true">
        insert into stock_block_rt_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="label != null">label,</if>
                <if test="blockName != null">block_name,</if>
                <if test="companyNum != null">company_num,</if>
                <if test="avgPrice != null">avg_price,</if>
                <if test="updownRate != null">updown_rate,</if>
                <if test="tradeAmount != null">trade_amount,</if>
                <if test="tradeVolume != null">trade_volume,</if>
                <if test="curTime != null">cur_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="label != null">#{label},</if>
                <if test="blockName != null">#{blockName},</if>
                <if test="companyNum != null">#{companyNum},</if>
                <if test="avgPrice != null">#{avgPrice},</if>
                <if test="updownRate != null">#{updownRate},</if>
                <if test="tradeAmount != null">#{tradeAmount},</if>
                <if test="tradeVolume != null">#{tradeVolume},</if>
                <if test="curTime != null">#{curTime},</if>
        </trim>
    </insert>
    <insert id="insertBatch">
        insert into stock_block_rt_info
        ( id,label,block_name
        ,company_num,avg_price,updown_rate
        ,trade_amount,trade_volume,cur_time
        )
        values
        <foreach collection="list" item="bi" separator=",">
            (#{bi.id,jdbcType=BIGINT},#{bi.label,jdbcType=VARCHAR},#{bi.blockName,jdbcType=VARCHAR}
            ,#{bi.companyNum,jdbcType=INTEGER},#{bi.avgPrice,jdbcType=DECIMAL},#{bi.updownRate,jdbcType=DECIMAL}
            ,#{bi.tradeAmount,jdbcType=BIGINT},#{bi.tradeVolume,jdbcType=DECIMAL},#{bi.curTime,jdbcType=TIMESTAMP}
            )
        </foreach>
        on duplicate key update
        id = values(id),
        label = values(label),
        block_name = values(block_name),
        company_num = values(company_num),
        avg_price = values(avg_price),
        updown_rate = values(updown_rate),
        trade_amount = values(trade_amount),
        trade_volume = values(trade_volume),
        cur_time = values(cur_time)
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.me.stock.pojo.entity.StockBlockRtInfo">
        update stock_block_rt_info
        <set>
                <if test="label != null">
                    label = #{label},
                </if>
                <if test="blockName != null">
                    block_name = #{blockName},
                </if>
                <if test="companyNum != null">
                    company_num = #{companyNum},
                </if>
                <if test="avgPrice != null">
                    avg_price = #{avgPrice},
                </if>
                <if test="updownRate != null">
                    updown_rate = #{updownRate},
                </if>
                <if test="tradeAmount != null">
                    trade_amount = #{tradeAmount},
                </if>
                <if test="tradeVolume != null">
                    trade_volume = #{tradeVolume},
                </if>
                <if test="curTime != null">
                    cur_time = #{curTime},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.me.stock.pojo.entity.StockBlockRtInfo">
        update stock_block_rt_info
        set 
            label =  #{label},
            block_name =  #{blockName},
            company_num =  #{companyNum},
            avg_price =  #{avgPrice},
            updown_rate =  #{updownRate},
            trade_amount =  #{tradeAmount},
            trade_volume =  #{tradeVolume},
            cur_time =  #{curTime}
        where   id = #{id} 
    </update>
</mapper>
